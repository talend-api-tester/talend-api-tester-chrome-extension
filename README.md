# talend-api-tester-chrome-extension
Talend Api Tester is a Chrome browser extension for sending HTTP requests and inspecting API responses. Talend Api Tester Chrome Extension supports custom headers, query parameters, variables, basic authentication, and assertions on status codes and response times. It runs from the browser toolbar.
